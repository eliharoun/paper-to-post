__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/
.env
data/
outputs/
.DS_Store
# Local Claude Code state — never commit anything under .claude/
.claude/
# Superpowers plan/spec docs — local only, kept out of the repo
docs/
# Composio CLI scratch files left by `composio run`
.composio-run-*
scripts/.composio-run-*
# Node (Composio CLI is invoked externally, not vendored)
node_modules/
